[package]
name = "swarm-models"
description = "Modelos ABM de referencia sobre swarm-core, reutilizables por ejemplos, bindings y benches"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
swarm-core = { workspace = true }
